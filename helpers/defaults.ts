import type { DefaultsInstance } from "vuetify"

export const defaults: DefaultsInstance = {
    VAppBar: {
		elevation: 0,
	},
	VTextField: {
		color: "primary",
		variant: "outlined",
		density: "comfortable",
	},
}